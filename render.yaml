services:
  - type: web
    name: covelligent
    runtime: node
    region: oregon
    plan: free
    buildCommand: npm install --omit=dev
    startCommand: node dist/index.cjs
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
