services:
  - type: web
    name: covelligent
    runtime: node
    region: oregon
    plan: free
    buildCommand: npm install && ./node_modules/.bin/tsx script/build.ts
    startCommand: node dist/index.cjs
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
